export const SERVER_URL:string = "http://localhost:3333";

const version:string = "/api/v1";
export const URLS = {
  AUTH: version + "/authS",
  USERS: version + "/users",
  ORDERS: version + "/blogs",
};